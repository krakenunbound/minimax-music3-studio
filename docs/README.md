# Docs

- [API keys](api-keys.md) — vault, Enable, Writing, what never goes to GitHub
- [Screenshots](screenshots/) — drop more captures here as `07-….png` / `.jpg` and link them from the root README

`outputs/settings/api-keys.json` and `models/` stay on disk only. They are gitignored.
